import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: ["Font"],
  },
});

// Material UI
export const styles = {
  textField: {
    fontSize: "1rem",
    fontWeight: "500",
    padding: "11px 14px",
    color: "#2a2a2a",
  },
};
